const {SUPABASE_URL,SUPABASE_KEY}=window.MODEX_CONFIG;const token=new URLSearchParams(location.search).get('t');
(async()=>{try{if(!token)throw new Error('QR noto‘g‘ri');const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/get_order_phone`,{method:'POST',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({p_token:token})});if(!r.ok)throw new Error(await r.text());const rows=await r.json(),d=Array.isArray(rows)?rows[0]:rows;if(!d)throw new Error('Topilmadi');qrName.textContent=d.full_name||'Buyurtma';qrRegion.textContent=d.region||'';qrPhone.textContent=d.phone;qrPhone.href=`tel:${d.phone}`;qrPhone.classList.remove('hidden');qrMessage.textContent='Telefon raqami';qrMessage.className='form-message success'}catch(e){qrName.textContent='Buyurtma topilmadi';qrMessage.textContent='QR kod yaroqsiz yoki buyurtma mavjud emas.';qrMessage.className='form-message error'}})();
